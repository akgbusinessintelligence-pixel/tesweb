# tesweb

Simple static HTML5 landing page for a cleaning company.

## Run locally

No Python or build tooling is required.

- Open `index.html` directly in your browser.
- Optional: serve the folder with any static file server you prefer.

## Files

- `index.html` — main landing page
- `404.html` — custom not-found page with redirect back to `index.html`
